# ShitRat CLI

See `AGENTS.md` for repo rules.

Important: this repo uses Bun, Effect CLI, and TypeScript 7 native preview (`tsgo`). Keep CLI output JSON-only and never print ShitRat private keys or installation tokens by default.
