// Optional pi-notes Brain pipeline config.
// Project-local components in .brain/components/**/*.svelte are auto-discovered by tag name.
// Add explicit components or safe remark/rehype plugins here only when needed.

export default {
  components: {
    // Example: ProjectCard: "components/ProjectCard.svelte",
  },
  remarkPlugins: [],
  rehypePlugins: [],
};
